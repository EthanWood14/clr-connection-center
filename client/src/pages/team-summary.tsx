import ManagerDashboard from "./manager-dashboard";

/** Replaces How we're doing while preserving its existing bookmarks. */
export default function TeamSummary() {
  return <ManagerDashboard view="overview" />;
}
